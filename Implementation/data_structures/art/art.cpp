#include "art.h"

namespace art {
    void art::insert(const uint32_t value) {

    }

    bool art::find(const uint32_t value) const {
        return false;
    }

    std::vector<uint32_t> art::find_range(const uint32_t from, const uint32_t to) const {
	    auto result = std::vector<uint32_t>();

        return result;
    }
}